{
  "routes": [
    {
      "method": "GET",
      "path": "/invitados",
      "handler": "invitados.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/invitados/count",
      "handler": "invitados.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/invitados/:id",
      "handler": "invitados.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/invitados",
      "handler": "invitados.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/invitados/:id",
      "handler": "invitados.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/invitados/:id",
      "handler": "invitados.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
